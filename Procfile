### Passo 2: Adicione um arquivo `Procfile`

Crie um arquivo chamado `Procfile` no diretório raiz do seu projeto e adicione a linha abaixo para especificar como iniciar o servidor.

web: npm start

### Verifique o `Procfile`

Certifique-se de que o `Procfile` está corretamente formatado e contém apenas a linha de comando necessária.

O erro "bash: linha 1: 0: comando não encontrado" sugere que há um problema com o comando de inicialização especificado no Render. Vamos garantir que tudo está configurado corretamente.
